**TCP / IP**
作为将构建不同设备之间网络连接的协议，需要完成三个步骤
* 由TCP完成一个双方向检测通道
* IP制定通道内节点与节点的连接规则，数据拆分，每一份数据独立，保留了开始节点和目标节点的地址信息，还有索引信息
* 接收端将每一个包解码并且拼接成为完整的格式

**多种传输堆栈**
**网络捆绑界面**
	各个协议之间底层是不互通的，若要构建互通则需要增加NBI这样的中间层，用来将上层协议解释为下一个协议看看得懂的协议格式就可以

**API**
* 