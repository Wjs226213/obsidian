# 计算机视觉学习笔记索引

本目录包含计算机视觉、OpenCV、YOLO、MediaPipe 和机器学习的学习笔记。

---

## OpenCV 基础

### 图像处理

- [[OpenCV 色彩空间]] - BGR、灰度、RGB、HSV 色彩空间转换
- [[OpenCV 图像调整大小]] - 图像缩放、裁剪、视频帧调整
- [[OpenCV 图像模糊处理]] - 均值模糊、高斯模糊、中值模糊
- [[OpenCV 阈值处理]] - 手动阈值、自适应阈值、批量阈值测试

---

## 目标检测与跟踪

### YOLO 目标检测

- [[YOLO + MediaPipe 手部检测]] - YOLO 目标检测 + MediaPipe 手部关键点检测

---

## 机器学习

### 深度学习

- [[CIFAR-10 + ResNet18 训练流程]] - ResNet18 在 CIFAR-10 上的训练流程

---

## 学习路径建议

### 阶段一：OpenCV 基础

1. 掌握色彩空间转换
2. 学习图像调整大小和裁剪
3. 理解图像模糊处理
4. 掌握阈值处理方法

### 阶段二：目标检测

1. 学习 YOLO 目标检测原理
2. 掌握 MediaPipe 手部关键点检测
3. 实现 YOLO + MediaPipe 结合应用

### 阶段三：深度学习

1. 理解 ResNet 残差网络结构
2. 学习 CIFAR-10 数据集处理
3. 掌握训练流程和超参数调优

---

## 代码示例位置

所有代码示例位于：
- `D:\develop\Py_project_list\FlaskProject\openccv____\` - OpenCV 示例代码
- `D:\develop\Py_project_list\FlaskProject\yolo_test\` - YOLO + MediaPipe 示例代码
- `D:\develop\Py_project_list\machine_learning\` - 机器学习训练代码

---

## 常用函数速查

### OpenCV

```python
# 色彩空间转换
cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)
cv2.cvtColor(img, cv2.COLOR_BGR2HSV)

# 图像调整
cv2.resize(img, (width, height))
img[y1:y2, x1:x2]  # 裁剪

# 图像模糊
cv2.blur(img, (kernel, kernel))
cv2.GaussianBlur(img, (kernel, kernel), 0)
cv2.medianBlur(img, kernel)

# 阈值处理
cv2.threshold(gray, thresh, maxval, type)
cv2.adaptiveThreshold(gray, maxval, method, type, blockSize, C)
```

### YOLO

```python
from ultralytics import YOLO
model = YOLO('yolov8n.pt')
results = model(image, conf=0.3)
```

### MediaPipe

```python
import mediapipe as mp
hands = mp.solutions.hands.Hands()
results = hands.process(rgb_image)
```

### PyTorch

```python
import torch
import torch.nn as nn
import torchvision.models as models

model = models.resnet18(weights=None)
model.fc = nn.Linear(model.fc.in_features, 10)
```